<label>{{title}}</label>
<section class="{{ ({{hasPrefix}}) ? 'input' : 'form' }}-group">
	@if ( {{hasPrefix}} )
    <span class="input-group-addon">{{prefix}}</span>
	@endif
    <input
    	type="text"
    	class="form-control"
    	name="{{name}}"
    	placeholder="{{title}}"
    	value="{{ ($errors && $errors->any() ? old('{{name}}') : (isset($item)? $item->{{name}} : '')) }}"
    	{{disabled}}
    >
</section>