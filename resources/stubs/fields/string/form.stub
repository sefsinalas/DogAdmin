<section class="form-group">
    <label>{{title}}</label>
    <input type="text" class="form-control" name="{{name}}" placeholder="{{title}}" value="{{ ($errors && $errors->any() ? old('description') : (isset($item)? $item->description : '')) }}">
</section>